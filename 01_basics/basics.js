
// Variable declaration
const accountId = 153524 // local inmutable variable -- Constant declaired 
var accountPassword = "HappyLearning" // least likely used, global mutable variable -- variable declaired 
let accountCity = "Delhi" // local mutable variable -- variable declaired 
accountName = "Vijay" // Not a good practice and not used anywhere
let accountState; // Undefined if you don't assign value

console.table([accountId, accountName, accountPassword, accountCity, accountState])
console.log("----------------------------------------------------------------------")

/* 
Premetive Data Types 
    int - number
    big int - large numbers
    string => "" or ''
    boolean => true/false
    null => standalone value
    undefined => 
    symbol => to find uniqueness
 */

let name = "Manuj"
let age = 24
let isLoggedin = true 

// Object
console.table(typeof ["Manuj"])
